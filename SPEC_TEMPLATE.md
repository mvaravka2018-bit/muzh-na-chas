# Спецификация фичи: [НАЗВАНИЕ]

## Описание
[Что делает, для кого, зачем. 2-3 предложения.]

## User Stories
- Как [роль], я хочу [действие], чтобы [результат]
- Как [роль], я хочу [действие], чтобы [результат]
- Как [роль], я хочу [действие], чтобы [результат]

## Модель данных
```sql
CREATE TABLE table_name (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  city_id    uuid NOT NULL REFERENCES cities(id),
  -- поля с типами
  created_at timestamptz NOT NULL DEFAULT now()
);
-- RLS политики
```

## API
```
METHOD /api/path
  body: {field: type}
  → 200: {response}
  → 400: {error: 'reason'}
  → 401: {error: 'unauthorized'}
```

## Экраны и компоненты
| Экран | Путь | Компоненты | Состояния |
|-------|------|-----------|----------|
| Название | `/path` | Component1, Component2 | loading, error, empty, success |

## Бизнес-логика
- Правило 1: [конкретное условие]
- Правило 2: [конкретное условие]

## Крайние случаи
- [Сценарий] → [Ожидаемое поведение]: {error: 'code'}
- [Сценарий] → [Ожидаемое поведение]

## Приоритет / Зависимости
- Приоритет: MVP / v2 / v3
- Зависит от: [другие модули]
- Блокирует: [другие модули]
